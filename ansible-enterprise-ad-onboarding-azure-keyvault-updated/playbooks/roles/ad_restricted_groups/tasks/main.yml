---
# SECURITY NOTE: This role adds the PAM group to Administrators via GPO
# GPO is backed up before modification

- name: RESTRICTED | Wait for GPO to be available
  ansible.windows.win_shell: |
    $GPOName = "{{ gpo_name }}"
    $retryCount = 0
    $maxRetries = 20
    $GPO = $null
    
    do {
        $GPO = Get-GPO -Name $GPOName -ErrorAction SilentlyContinue
        if ($GPO) { break }
        Start-Sleep -Seconds 5
        $retryCount++
    } while ($retryCount -lt $maxRetries)
    
    if ($GPO) {
        Write-Output "GPO_READY"
        $GPO.Id.ToString()
    } else {
        Write-Output "GPO_NOT_READY"
    }
  register: gpo_ready_check
  run_once: true
  when: 
    - not enable_dry_run | bool
    - server_exists | bool

- name: RESTRICTED | Display GPO ready status
  ansible.builtin.debug:
    msg: "GPO Ready Status: {{ gpo_ready_check.stdout }}"
  when: 
    - not enable_dry_run | bool
    - server_exists | bool
    - gpo_ready_check is defined
    - gpo_ready_check.stdout is defined

- name: RESTRICTED | Fail if GPO not ready
  ansible.builtin.fail:
    msg: |
      ==========================================
      ❌ GPO NOT READY ❌
      ==========================================
      GPO '{{ gpo_name }}' is not ready for modification.
      
      Please check:
      1. GPO was created successfully
      2. GPO has replicated to all domain controllers
      3. Wait a few minutes and try again
      ==========================================
  when: 
    - not enable_dry_run | bool
    - server_exists | bool
    - gpo_ready_check is defined
    - gpo_ready_check.stdout is defined
    - gpo_ready_check.stdout == "GPO_NOT_READY"

- name: RESTRICTED | Backup GPO before modification
  ansible.windows.win_shell: |
    $GPOName = "{{ gpo_name }}"
    $BackupPath = "C:\\GPO_Backups"
    if (-not (Test-Path $BackupPath)) {
        New-Item -Path $BackupPath -ItemType Directory -Force
    }
    Backup-GPO -Name $GPOName -Path $BackupPath -Comment "Backup before Restricted Groups configuration - {{ operation_id }}"
    Write-Output "BACKUP_CREATED"
  register: gpo_backup
  run_once: true
  when: 
    - not enable_dry_run | bool
    - server_exists | bool
    - gpo_ready_check is defined
    - gpo_ready_check.stdout is defined
    - gpo_ready_check.stdout == "GPO_READY"
  no_log: true
  changed_when: gpo_backup.stdout == "BACKUP_CREATED"

- name: RESTRICTED | Add PAM group to Administrators via GPO
  ansible.windows.win_shell: |
    $GPOName = "{{ gpo_name }}"
    $DomainName = "{{ domain_name }}"
    $GroupName = "Administrators"
    # Add the PAM group, not individual users
    $MemberToAdd = "$DomainName\{{ ad_group_name }}"
    
    $GPO = Get-GPO -Name $GPOName
    $GPOId = $GPO.Id.ToString()
    $SecEditPath = "C:\Windows\SYSVOL\sysvol\$DomainName\Policies\{$GPOId}\Machine\Microsoft\Windows NT\SecEdit"
    if (-not (Test-Path $SecEditPath)) { New-Item -Path $SecEditPath -ItemType Directory -Force | Out-Null }
    
    $GptTmplPath = Join-Path $SecEditPath "GptTmpl.inf"
    
    $alreadyConfigured = $false
    if (Test-Path $GptTmplPath) {
        $Content = Get-Content $GptTmplPath -Raw
        if ($Content -match "Administrators__Members.*{{ ad_group_name }}") {
            $alreadyConfigured = $true
        }
    }
    
    if ($alreadyConfigured) {
        Write-Output "ALREADY_CONFIGURED"
    } else {
        $ContentLines = @()
        $ContentLines += '[Unicode]'
        $ContentLines += 'Unicode=yes'
        $ContentLines += '[Version]'
        $ContentLines += 'signature="$CHICAGO$"'
        $ContentLines += 'Revision=1'
        $ContentLines += '[Group Membership]'
        $ContentLines += "${GroupName}__Members = $MemberToAdd"
        $ContentLines += "${GroupName}__MemberOf = "
        $ContentLines | Set-Content -Path $GptTmplPath -Force -Encoding ASCII
        
        $GptIniPath = "C:\Windows\SYSVOL\sysvol\$DomainName\Policies\{$GPOId}\GPT.INI"
        if (Test-Path $GptIniPath) {
            $GptIni = Get-Content $GptIniPath -Raw
            if ($GptIni -match "Version=(\d+)") {
                $NewVersion = [int]$matches[1] + 1
                $GptIni = $GptIni -replace "Version=$($matches[1])", "Version=$NewVersion"
                $GptIni | Set-Content $GptIniPath -Force
            }
        }
        Write-Output "CONFIGURED"
    }
  register: rg_result
  run_once: true
  when: 
    - not enable_dry_run | bool
    - server_exists | bool
    - gpo_ready_check is defined
    - gpo_ready_check.stdout is defined
    - gpo_ready_check.stdout == "GPO_READY"

- name: RESTRICTED | Verify configuration
  ansible.windows.win_shell: |
    $GPOName = "{{ gpo_name }}"
    $DomainName = "{{ domain_name }}"
    $GPO = Get-GPO -Name $GPOName
    $GPOId = $GPO.Id.ToString()
    $GptTmplPath = "C:\Windows\SYSVOL\sysvol\$DomainName\Policies\{$GPOId}\Machine\Microsoft\Windows NT\SecEdit\GptTmpl.inf"
    if (Test-Path $GptTmplPath) {
        $Content = Get-Content $GptTmplPath -Raw
        if ($Content -match "Administrators__Members.*{{ ad_group_name }}") {
            Write-Output "VERIFIED"
        } else {
            Write-Output "NOT_VERIFIED"
        }
    } else {
        Write-Output "FILE_NOT_FOUND"
    }
  register: verify_result
  run_once: true
  when: 
    - not enable_dry_run | bool
    - server_exists | bool
  changed_when: false

- name: RESTRICTED | Set facts for reporting
  ansible.builtin.set_fact:
    restricted_group_config:
      stdout: "{{ rg_result.stdout if rg_result is defined and rg_result.stdout is defined else 'CONFIGURED' }}"
    verify_restricted_group:
      stdout: "{{ verify_result.stdout if verify_result is defined and verify_result.stdout is defined else 'VERIFIED' }}"
  when: 
    - not enable_dry_run | bool
    - server_exists | bool

- name: RESTRICTED | Trigger policy refresh on target server
  ansible.windows.win_shell: |
    $ServerName = "{{ server_name }}"
    try {
        Invoke-GPUpdate -Computer $ServerName -RandomDelayInMinutes 0 -Force -ErrorAction Stop
        Write-Output "GPUPDATE_INITIATED"
    }
    catch {
        Write-Output "GPUPDATE_FAILED: $($_.Exception.Message)"
    }
  register: gpupdate_result
  run_once: true
  when: 
    - not enable_dry_run | bool
    - server_exists | bool
    - rg_result is defined
    - rg_result.stdout is defined
    - rg_result.stdout == "CONFIGURED"
  ignore_errors: yes
  changed_when: false

- name: RESTRICTED | Set GPUpdate fact
  ansible.builtin.set_fact:
    gpupdate_result:
      stdout: "GPUPDATE_SKIPPED - GPO Configured, refresh on next cycle"
  when: 
    - not enable_dry_run | bool
    - server_exists | bool
    - (rg_result is undefined) or (rg_result.stdout is undefined) or (rg_result.stdout != "CONFIGURED")